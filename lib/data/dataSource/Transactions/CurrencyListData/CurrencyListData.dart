import 'package:airtop/AllImports.dart';

class CurrencyListData extends StatelessWidget {
  CurrencyListData({Key? key}) : super(key: key);

  List<TransactionCurrencyListModel> currencyDataList = [];
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
